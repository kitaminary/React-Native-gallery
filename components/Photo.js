import React from "react";
import { Modal, StyleSheet } from "react-native";

export default function Photo() {
  return <Modal></Modal>;
}

const styles = StyleSheet.create({
  PhotoBox: {
    width: 200,
    height: 200,
    backgroundColor: "#446995",
    borderRadius: 20,
  },
});
